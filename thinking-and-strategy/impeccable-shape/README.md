# 🧩 /impeccable shape

> Part of the **Thinking & Strategy** collection.

| | |
|---|---|
| **Source** | [impeccable.style/docs/shape](https://impeccable.style/docs/shape) |
| **Invoke** | `/impeccable shape` |
| **Status** | 🔗 Catalog-only — install from the source (not redistributed here) |

## What it does
Runs a structured discovery interview before any UI work begins. Asks targeted questions about purpose, users, content, constraints, and intent — then produces a `brief.md` design brief that implementation skills (like `/impeccable` or `/impeccable craft`) can read before writing a single line of code.

## When to use it
Before a feature starts. When a ticket is vague, when the team is about to argue about UI before aligning on intent, or when you catch yourself writing code to figure out what the product should actually do.

## Why it matters
Most AI-generated UIs fail not because of bad code, but because of skipped thinking. The model jumps straight to "here is a card grid" without asking what the user is trying to accomplish. `/impeccable shape` inverts that order.

## Curator's take
> The interview is maybe 5 minutes. The rewrites it prevents are measured in hours. The brief it produces isn't a spec — it's a compass. That distinction matters: it captures intent, not UI, which leaves room for the implementation to be good.

## How to get it
Part of the **Impeccable** toolkit. Install from [impeccable.style](https://impeccable.style/docs/shape). Not vendored here because it ships as a product, not an open SKILL.md.
